# CVScope Backend — Pinned Dependencies
# ======================================
# Pin ALL versions so both machines produce IDENTICAL results.
# To set up from scratch:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
#
# To verify your environment matches: GET http://127.0.0.1:8000/env

fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.24
starlette==1.0.0

# NLP
spacy==3.8.14
# spaCy model is installed separately (see above)

# PDF / DOCX extraction
pdfplumber==0.11.9
python-docx==1.2.0

# Skill matching & scoring
rapidfuzz==3.14.5
scikit-learn==1.8.0

# Optional keyphrase extraction (taxonomy-guarded, safe to include)
# Set env var CVSCOPE_DISABLE_YAKE=1 to turn off without uninstalling
yake==0.7.3
